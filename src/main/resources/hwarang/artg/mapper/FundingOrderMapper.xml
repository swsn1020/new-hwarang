<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hwarang.artg.mapper.FundingOrderMapper">

	<select id="selectOne" resultType="hwarang.artg.funding.model.OrderVO">
		select * from funding_order 
		where order_seq_num = #{order_seq_num}
	</select>
	
	<select id="selectAll" resultType="hwarang.artg.funding.model.OrderVO">
		select * from funding_order
	</select>

	<select id="getFunding">
		select * from crowdfunding
	</select>
	<insert id="insertOrder">
		<selectKey keyProperty="order_seq_num" order="BEFORE" resultType="int">
			SELECT order_seq_num.nextVal
            FROM dual
		</selectKey>
		INSERT INTO funding_order (order_seq_num,order_name,order_price,order_id,member_id,order_rec,member_address,order_phone,amount,order_date)
		VALUES
		(order_seq_num.nextval, #{order_seq_num},#{order_name},#{order_price},#{order_id},#{member_id}
		,#{order_rec},#{member_address},#{order_phone},#{amount},#{order_date})
	</insert>
	
</mapper>